# 线上环境
ENV = 'production'

# 线上环境接口地址
VITE_API_URL = '/api' # docker-compose部署不需要修改，nginx容器自动代理了这个地址

# LLM 分析服务地址（生产环境，需根据实际部署情况修改）
VITE_LLM_API_BASE_URL = 'http://your-llm-service-url'

# 是否启用按钮权限
VITE_PM_ENABLED = true
