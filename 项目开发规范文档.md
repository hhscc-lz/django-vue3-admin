# 项目开发规范文档

> 基于长春市12345情感分析平台后台管理系统
>
> 版本：v2.0
>
> 生成时间：2025-11-27
>
> 更新时间：2025-11-27

---

## 📋 目录

1. [技术栈概览](#技术栈概览)
2. [架构设计原则](#架构设计原则)
3. [后端开发规范](#后端开发规范)
4. [前端开发规范](#前端开发规范)
5. [通用组件开发规范](#通用组件开发规范)
6. [数据库规范](#数据库规范)
7. [命名规范](#命名规范)
8. [文件组织规范](#文件组织规范)
9. [完整开发流程示例](#完整开发流程示例)
10. [常见错误和解决方案](#常见错误和解决方案)

---

## 技术栈概览

### 后端技术栈
- **框架**: Django 4.2.14
- **API**: Django REST Framework 3.15.2
- **数据库**: MySQL 8.0（统一使用默认数据库）
- **基础类**: `CustomModelViewSet`, `CustomModelSerializer`

### 前端技术栈
- **框架**: Vue 3 (Composition API)
- **UI库**: Element Plus
- **CRUD框架**: Fast-CRUD
- **语言**: TypeScript
- **路由**: Vue Router

---

## 架构设计原则

### 1. 插件独立性原则

**核心思想：** 通用功能应独立成单独的插件，避免耦合到特定业务模块。

#### 1.1 为什么需要独立插件？

**❌ 反例：**
```
plugins/
└── risk_warning/          # 风险预警插件
    ├── models.py          # 包含 Complaint 和 RiskTag 模型
    ├── serializers.py     # 包含工单和风险的序列化器
    └── views.py           # 包含工单和风险的视图
```

**问题：**
- 其他功能（情感分析、统计分析等）也需要查看工单详情
- 必须依赖 risk_warning 插件，造成不合理的耦合
- 工单相关代码混杂在风险预警中，职责不清晰

**✅ 正确做法：**
```
plugins/
├── complaint/             # 【独立】工单管理插件
│   ├── models.py          # Complaint 模型
│   ├── serializers.py     # 工单序列化器
│   └── views.py           # 工单详情接口
└── risk_warning/          # 风险预警插件
    ├── models.py          # RiskTag 模型（引用 complaint）
    ├── serializers.py     # 风险序列化器
    └── views.py           # 风险视图
```

**优势：**
- ✅ **单一职责** - 每个插件职责明确
- ✅ **高内聚低耦合** - complaint 可独立使用
- ✅ **易于复用** - 任何模块都可以引用工单功能
- ✅ **易于维护** - 工单相关修改只需修改一处

#### 1.2 判断是否需要独立插件

问自己三个问题：

1. **是否有多个功能需要使用？** - 如果是，独立出来
2. **是否是核心业务数据？** - 如果是，独立出来
3. **是否与特定功能强绑定？** - 如果否，独立出来

**示例：**
- `Complaint` (工单) → **独立** - 多个功能都需要查看工单
- `RiskTag` (风险标注) → **不独立** - 仅风险预警使用
- `SentimentAnalysis` (情感分析) → **独立** - 核心业务数据，多处使用
- `User` (用户) → **独立** - 系统核心，全局使用

---

### 2. 数据库设计原则

#### 2.1 统一数据库配置

**⚠️ 重要变更：** 项目已移除多数据库路由配置，统一使用默认数据库。

**配置示例：**
```python
# application/settings.py

DATABASES = {
    "default": {
        "ENGINE": DATABASE_ENGINE,
        "NAME": DATABASE_NAME,
        "USER": DATABASE_USER,
        "PASSWORD": DATABASE_PASSWORD,
        "HOST": DATABASE_HOST,
        "PORT": DATABASE_PORT,
    }
}

# ❌ 不再使用多数据库配置
# DATABASE_ROUTERS = ['application.db_router.IntelligenceDBRouter']
```

**关键要点：**
1. **所有模型** 使用默认数据库
2. **不需要** `.using('intelligence')` 指定数据库
3. **ForeignKey 关系** 可以正常使用，无跨库问题
4. **查询优化** 使用 `select_related` 和 `prefetch_related`

---

## 后端开发规范

### 1. 数据模型（Models）

#### 1.1 基本规范

**✅ 标准示例：**
```python
"""
模块名称：风险预警模块
@author: 开发者姓名
@contact: QQ/邮箱
@Created on: 2025-11-27
@Remark: 功能说明
"""
from django.db import models
from dvadmin.utils.models import CoreModel

class RiskTag(models.Model):
    """
    风险标注表

    映射到 intelligence 数据库的 risk_tags 表
    """
    complaint = models.ForeignKey(
        'Complaint',
        on_delete=models.DO_NOTHING,  # 只读模式
        db_column='complaint_id',
        to_field='id',
        related_name='risk_tags',
        verbose_name='工单'
    )
    is_risk = models.BooleanField(
        default=False,
        verbose_name='是否风险'
    )
    risk_category = models.CharField(
        max_length=50,
        null=True,
        blank=True,
        verbose_name='风险类别'
    )
    created_at = models.DateTimeField(
        auto_now_add=True,
        verbose_name='创建时间'
    )

    class Meta:
        db_table = 'risk_tags'
        managed = False  # 映射现有表，不由 Django 管理
        verbose_name = '风险标注'
        verbose_name_plural = verbose_name
        ordering = ['-created_at']

    def __str__(self):
        return f"{self.complaint_id} - {self.risk_category}"
```

#### 1.2 关键要点

1. **模块注释** - 必须包含作者、时间、功能说明
2. **字段 verbose_name** - 所有字段必须有中文名称
3. **Meta 配置** - 必须包含 db_table, verbose_name, ordering
4. **只读映射** - 使用 `managed = False` 映射现有表
5. **外键配置** - 使用 `on_delete=models.DO_NOTHING` 用于只读关系
6. **__str__ 方法** - 提供清晰的字符串表示

---

### 2. 序列化器（Serializers）

#### 2.1 基本规范

**✅ 标准示例：**
```python
"""
风险预警序列化器
"""
from rest_framework import serializers
from dvadmin.utils.serializers import CustomModelSerializer
from .models import RiskTag

class RiskTagSerializer(CustomModelSerializer):
    """
    风险标注序列化器 (包含工单信息)

    继承 CustomModelSerializer 以获得：
    - 自动时间格式化 (YYYY-MM-DD HH:mm:ss)
    - 中文错误提示
    - 动态字段查询支持
    - 审计字段自动处理
    """
    # 工单ID
    complaint_id = serializers.CharField(
        source='complaint.id',
        read_only=True
    )

    # 嵌套工单字段（允许为空）
    complaint_title = serializers.CharField(
        source='complaint.title',
        read_only=True,
        required=False,
        allow_null=True,
        allow_blank=True
    )

    class Meta:
        model = RiskTag
        fields = [
            'id', 'complaint_id', 'is_risk', 'risk_category',
            'risk_reason', 'created_at',
            'complaint_title', 'complaint_region'
        ]
        read_only_fields = fields  # 只读视图

class RiskTagExportSerializer(CustomModelSerializer):
    """
    风险标注导出序列化器 (用于Excel导出)

    使用中文字段名
    """
    工单编号 = serializers.CharField(source='complaint.id')
    是否风险 = serializers.SerializerMethodField()
    风险类别 = serializers.CharField(source='risk_category')

    class Meta:
        model = RiskTag
        fields = ['工单编号', '是否风险', '风险类别']

    def get_是否风险(self, obj):
        return '是' if obj.is_risk else '否'
```

#### 2.2 关键要点

1. **⚠️ 必须继承 `CustomModelSerializer`** - 项目标准基类
2. **NULL 值处理** - 添加 `required=False, allow_null=True, allow_blank=True`
3. **嵌套字段** - 使用 `source='relation.field'` 访问关联对象
4. **SerializerMethodField** - 用于复杂逻辑处理
5. **导出序列化器** - 单独创建，使用中文字段名
6. **详细注释** - 说明继承基类的好处

---

### 3. 视图集（ViewSets）

#### 3.1 基本规范

**✅ 标准示例：**
```python
"""
风险预警视图
"""
from rest_framework import viewsets
from rest_framework.decorators import action
from rest_framework.response import Response
from django_filters import rest_framework as filters
from openpyxl import Workbook
from django.http import HttpResponse
from datetime import datetime

from .models import RiskTag
from .serializers import RiskTagSerializer, RiskTagExportSerializer
from dvadmin.utils.viewset import CustomModelViewSet

class RiskTagFilter(filters.FilterSet):
    """
    风险标注过滤器 (按工单的受理时间筛选)
    """
    # 按工单受理时间筛选
    accept_time_start = filters.DateTimeFilter(
        field_name='complaint__accept_time',
        lookup_expr='gte',
        label='受理时间开始'
    )
    accept_time_end = filters.DateTimeFilter(
        field_name='complaint__accept_time',
        lookup_expr='lte',
        label='受理时间结束'
    )

    # 按风险类别筛选
    risk_category = filters.CharFilter(
        field_name='risk_category',
        lookup_expr='exact',
        label='风险类别'
    )

    class Meta:
        model = RiskTag
        fields = [
            'accept_time_start', 'accept_time_end',
            'risk_category', 'is_risk'
        ]

class RiskTagViewSet(CustomModelViewSet):
    """
    风险预警视图集 (只读)

    list:查询风险标注列表 | retrieve:查询单个风险标注详情 | export:导出Excel
    """
    queryset = RiskTag.objects.select_related('complaint').all()
    serializer_class = RiskTagSerializer
    filterset_class = RiskTagFilter
    search_fields = ['complaint__id', 'risk_category', 'risk_reason']
    ordering_fields = ['created_at', 'complaint__accept_time']
    ordering = ['-created_at']

    # 禁用创建、更新、删除操作
    http_method_names = ['get', 'head', 'options']

    def get_queryset(self):
        """
        优化查询: 预加载关联的工单信息
        """
        queryset = super().get_queryset()
        return queryset.select_related('complaint')

    @action(methods=['get'], detail=False, url_path='export')
    def export_data(self, request, *args, **kwargs):
        """
        导出风险预警数据 (包含完整工单信息)
        """
        # 获取筛选后的查询集
        queryset = self.filter_queryset(self.get_queryset())

        # 使用导出序列化器
        serializer = RiskTagExportSerializer(queryset, many=True)
        data = serializer.data

        # 创建 Excel 工作簿
        wb = Workbook()
        ws = wb.active
        ws.title = '风险预警数据'

        # 写入表头
        if data:
            headers = list(data[0].keys())
            ws.append(headers)

            # 写入数据
            for item in data:
                row = [item.get(header, '') for header in headers]
                ws.append(row)

        # 生成响应
        response = HttpResponse(
            content_type='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
        )
        filename = f'风险预警数据_{datetime.now().strftime("%Y%m%d_%H%M%S")}.xlsx'
        response['Content-Disposition'] = f'attachment; filename="{filename}"'

        wb.save(response)
        return response

    def create(self, request, *args, **kwargs):
        """禁用创建操作"""
        return Response({
            'code': 4000,
            'msg': '风险预警数据为只读，不支持创建操作'
        }, status=403)
```

#### 3.2 关键要点

1. **⚠️ 必须继承 `CustomModelViewSet`** - 项目标准基类
2. **过滤器配置** - 使用 `FilterSet` 实现复杂筛选
3. **跨表查询** - 使用 `field_name='relation__field'`
4. **查询优化** - 使用 `select_related` 预加载关联数据
5. **自定义 action** - 使用 `@action` 装饰器添加自定义接口
6. **只读设计** - 使用 `http_method_names` 限制方法
7. **操作说明** - 在 docstring 中说明支持的操作

---

### 4. URL 配置

#### 4.1 基本规范

**✅ 标准示例：**
```python
"""
风险预警路由配置
"""
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from .views import RiskTagViewSet

router = DefaultRouter()
router.register(r'risk-tags', RiskTagViewSet, basename='risk-tags')

urlpatterns = [
    path('', include(router.urls)),
]
```

#### 4.2 在 settings.py 中注册

```python
# application/settings.py

INSTALLED_APPS = [
    # ...
    "plugins.risk_warning",  # 添加插件应用
]

# 插件 URL 配置
PLUGINS_URL_PATTERNS = [
    {
        're_path': r'^api/risk_warning/',
        'include': 'plugins.risk_warning.urls'
    }
]
```

#### 4.3 关键要点

1. **URL 命名** - 使用小写 + 连字符（kebab-case）
2. **basename** - 必须指定，用于反向解析
3. **插件注册** - 在 `PLUGINS_URL_PATTERNS` 中注册

---

### 5. 多数据库配置

#### 5.1 数据库路由器

**✅ 标准示例：**
```python
"""
数据库路由器
"""
class IntelligenceDBRouter:
    """
    智能分析数据库路由器

    将 risk_warning 应用的所有操作路由到 intelligence 数据库
    """
    intelligence_apps = {'risk_warning'}

    def db_for_read(self, model, **hints):
        """读操作路由"""
        if model._meta.app_label in self.intelligence_apps:
            return 'intelligence'
        return None

    def db_for_write(self, model, **hints):
        """写操作路由"""
        if model._meta.app_label in self.intelligence_apps:
            return 'intelligence'
        return None

    def allow_relation(self, obj1, obj2, **hints):
        """允许关系"""
        if (obj1._meta.app_label in self.intelligence_apps or
            obj2._meta.app_label in self.intelligence_apps):
            return True
        return None

    def allow_migrate(self, db, app_label, model_name=None, **hints):
        """禁止迁移"""
        if app_label in self.intelligence_apps:
            return db == 'intelligence'
        return None
```

#### 5.2 在 settings.py 中配置

```python
# application/settings.py

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": "dvadmin",
        # ...
    },
    "intelligence": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": "changchun",
        "USER": DATABASE_USER,
        "PASSWORD": DATABASE_PASSWORD,
        "HOST": DATABASE_HOST,
        "PORT": DATABASE_PORT,
    }
}

DATABASE_ROUTERS = ['application.db_router.IntelligenceDBRouter']
```

---

## 前端开发规范

### 1. 目录结构

**✅ 标准结构：**
```
web/src/views/risk/warning/
├── index.vue          # 主页面组件
├── crud.tsx           # CRUD 配置（核心）
└── api.ts             # API 接口定义
```

**⚠️ 必须遵循三文件结构！**

---

### 2. API 接口层（api.ts）

#### 2.1 基本规范

**✅ 标准示例：**
```typescript
/**
 * 风险预警 API
 */
import { request } from '/@/utils/service'
import type { PageQuery, PageRes, InfoReq } from '@fast-crud/fast-crud'

// API 基础路径
export const apiPrefix = '/api/risk_warning/risk-tags'

/**
 * 数据接口定义
 */
export interface RiskTag {
  id: number
  complaint_id: string
  is_risk: boolean
  risk_category: string | null
  risk_reason: string | null
  created_at: string
  // 关联的工单字段
  complaint_title: string | null
  complaint_region: string | null
}

/**
 * 查询参数接口
 */
export interface RiskTagQuery extends PageQuery {
  accept_time_start?: string
  accept_time_end?: string
  accept_time_range?: string[]  // 前端用
  risk_category?: string
  is_risk?: boolean
  complaint_id?: string
}

/**
 * 获取列表
 */
export function GetList(query: RiskTagQuery): Promise<PageRes<RiskTag>> {
  return request({
    url: apiPrefix + '/',
    method: 'get',
    params: query
  })
}

/**
 * 获取详情
 */
export function GetObj(id: InfoReq): Promise<RiskTag> {
  return request({
    url: apiPrefix + `/${id}/`,
    method: 'get'
  })
}

/**
 * 导出数据
 */
export async function Export(query: RiskTagQuery): Promise<Blob> {
  const response: any = await request({
    url: apiPrefix + '/export/',
    method: 'get',
    params: query,
    responseType: 'blob'
  })
  return response.data || response
}

/**
 * 导出文件下载助手
 */
export function downloadExportFile(blob: Blob, filename?: string) {
  const url = window.URL.createObjectURL(blob)
  const link = document.createElement('a')
  link.href = url
  link.download = filename || `风险预警数据_${new Date().getTime()}.xlsx`
  document.body.appendChild(link)
  link.click()
  document.body.removeChild(link)
  window.URL.revokeObjectURL(url)
}
```

#### 2.2 关键要点

1. **⚠️ 必须使用独立函数导出** - 不要用对象导出
2. **函数命名规范** - `GetList`, `GetObj`, `AddObj`, `UpdateObj`, `DelObj`, `Export`
3. **apiPrefix 导出** - 使用 `export const` 导出
4. **类型定义** - 使用 TypeScript 接口定义
5. **Blob 处理** - `responseType: 'blob'` 时返回 `response.data`

---

### 3. CRUD 配置层（crud.tsx）

#### 3.1 基本规范

**✅ 标准示例：**
```typescript
/**
 * 风险预警 CRUD 配置
 */
import {
  CrudOptions,
  CrudExpose,
  CreateCrudOptionsRet,
  CreateCrudOptionsProps,
  dict
} from '@fast-crud/fast-crud'
import * as api from './api'
import type { RiskTagQuery } from './api'
import { downloadExportFile } from './api'
import { ElMessage } from 'element-plus'
import dayjs from 'dayjs'
import { dictionary } from '/@/utils/dictionary'

export const createCrudOptions = function ({ crudExpose }: CreateCrudOptionsProps): CreateCrudOptionsRet {
  // 页面请求
  const pageRequest = async (query: RiskTagQuery) => {
    // ✅ 在这里统一处理默认过滤和参数转换
    const requestQuery: any = {
      ...query,
      is_risk: true  // 默认过滤条件
    }

    // 处理时间范围参数转换
    if (query.accept_time_range && Array.isArray(query.accept_time_range) && query.accept_time_range.length === 2) {
      requestQuery.accept_time_start = query.accept_time_range[0]
      requestQuery.accept_time_end = query.accept_time_range[1]
      delete requestQuery.accept_time_range
    }

    return await api.GetList(requestQuery)
  }

  // 导出请求
  const exportRequest = async (formData: any) => {
    try {
      const query: RiskTagQuery = {
        is_risk: true,
        ...formData
      }

      if (formData.accept_time_range && formData.accept_time_range.length === 2) {
        query.accept_time_start = formData.accept_time_range[0]
        query.accept_time_end = formData.accept_time_range[1]
        delete query.accept_time_range
      }

      const blob = await api.Export(query)
      downloadExportFile(blob)
      ElMessage.success('导出成功')
    } catch (error) {
      ElMessage.error('导出失败')
      console.error(error)
    }
  }

  return {
    crudOptions: {
      request: {
        pageRequest,
        // ⚠️ 不要使用 transformQuery 和 transformRes
        // 使用 responseMap 配置字段映射
        responseMap: {
          data: 'data',           // 数据列表字段
          currentPage: 'page',    // 当前页字段
          pageSize: 'limit',      // 每页数量字段
          total: 'total'          // 总数字段
        }
      },

      // 操作栏配置
      actionbar: {
        buttons: {
          add: { show: false },  // 隐藏新增按钮
          export: {
            text: '导出',
            type: 'primary',
            click: () => {
              return exportRequest(crudExpose.getSearchFormData())
            }
          }
        }
      },

      // 行操作配置
      rowHandle: {
        fixed: 'right',
        width: 100,
        buttons: {
          view: {
            show: true,
            text: '查看',
            type: 'text'
          },
          edit: { show: false },
          remove: { show: false }
        }
      },

      // 表格配置
      table: {
        size: 'small',
        stripe: true,
        border: true
      },

      // 分页配置
      pagination: {
        pageSize: 20,
        pageSizes: [10, 20, 50, 100]
      },

      // 查看表单配置
      viewForm: {
        wrapper: {
          is: 'el-dialog',
          width: '900px',
          top: '5vh'
        },
        labelWidth: '120px',
        labelPosition: 'right',
        group: {
          groups: {
            riskInfo: {
              title: '风险信息',
              columns: ['risk_category', 'risk_reason', 'created_at']
            },
            complaintBasic: {
              title: '工单基础信息',
              columns: ['complaint_id', 'complaint_title', 'complaint_region']
            }
          }
        }
      },

      // 字段配置
      columns: {
        // 工单编号
        complaint_id: {
          title: '工单编号',
          type: 'text',
          search: {
            show: true
          },
          column: {
            width: 150,
            sortable: false
          },
          form: { show: false },
          viewForm: { show: true }
        },

        // 风险类别
        risk_category: {
          title: '风险类别',
          type: 'dict-select',  // ✅ 使用字典类型
          dict: dict({
            data: dictionary('risk_category')  // ✅ 从系统字典获取
          }),
          search: {
            show: true
          },
          column: {
            width: 130,
            sortable: false
          },
          form: { show: false },
          viewForm: { show: true }
        },

        // 受理时间范围（搜索）
        accept_time_range: {
          title: '受理时间',
          type: 'datetime-range',
          search: {
            show: true,
            component: {
              name: 'el-date-picker',
              type: 'datetimerange',
              startPlaceholder: '开始时间',
              endPlaceholder: '结束时间',
              valueFormat: 'YYYY-MM-DD HH:mm:ss'
            }
          },
          column: { show: false },
          form: { show: false }
        },

        // 受理时间（列显示）
        complaint_accept_time: {
          title: '受理时间',
          type: 'datetime',
          search: { show: false },
          column: {
            width: 160,
            sortable: 'custom',
            formatter: ({ value }: any) => {
              return value ? dayjs(value).format('YYYY-MM-DD HH:mm') : ''
            }
          },
          form: { show: false },
          viewForm: { show: true }
        }
      }
    }
  }
}
```

#### 3.2 关键要点

1. **⚠️ 命名导出** - `export const createCrudOptions`
2. **⚠️ 使用 CreateCrudOptionsProps** - 类型安全
3. **⚠️ 不要使用 transformQuery/transformRes** - 使用 responseMap
4. **参数转换** - 在 pageRequest 中处理
5. **字典系统** - 使用 `dict(dictionary('key'))`
6. **导入方式** - `import * as api from './api'`

---

### 4. 主页面组件（index.vue）

#### 4.1 基本规范

**✅ 标准示例：**
```vue
<template>
  <fs-page class="risk-warning-container">
    <fs-crud ref="crudRef" v-bind="crudBinding" />
  </fs-page>
</template>

<script lang="ts" setup name="riskWarning">
import { onMounted, ref } from 'vue'
import { useExpose, useCrud } from '@fast-crud/fast-crud'
import { createCrudOptions } from './crud'

// crud组件的ref
const crudRef = ref()
// crud 配置的ref
const crudBinding = ref()
// 暴露的方法
const { crudExpose } = useExpose({ crudRef, crudBinding })
// 你的crud配置
const { crudOptions } = createCrudOptions({ crudExpose })
// 初始化crud配置
const { resetCrudOptions } = useCrud({ crudExpose, crudOptions })

// 页面打开后获取列表数据
onMounted(() => {
  crudExpose.doRefresh()
})
</script>

<style scoped lang="scss">
.risk-warning-container {
  :deep(.fs-crud) {
    .el-card {
      border: none;
      box-shadow: none;
    }
  }
}
</style>
```

#### 4.2 关键要点

1. **⚠️ 使用 `<script setup>`** - Vue 3 推荐语法
2. **⚠️ 三步初始化流程**：
   - 步骤1：`useExpose({ crudRef, crudBinding })` 创建 crudExpose
   - 步骤2：`createCrudOptions({ crudExpose })` 创建配置
   - 步骤3：`useCrud({ crudExpose, crudOptions })` 初始化
3. **name 属性** - 在 `<script setup name="xxx">` 中指定
4. **导入方式** - 使用命名导入 `{ createCrudOptions }`

---

### 5. 字典管理

#### 5.1 在后台管理界面创建字典

1. 登录后台管理系统
2. 进入 **系统管理 > 字典管理**
3. 创建父字典：
   - 字典名称：`风险类别`
   - 字典编码：`risk_category` ← 这是前端使用的 key
   - 状态：启用
4. 创建子字典：
   - 依次添加字典项（标签、值、排序）

#### 5.2 前端使用字典

```typescript
import { dict } from '@fast-crud/fast-crud'
import { dictionary } from '/@/utils/dictionary'

// 在字段配置中使用
risk_category: {
  type: 'dict-select',
  dict: dict({
    data: dictionary('risk_category')  // ✅ 使用系统字典
  })
}
```

#### 5.3 关键要点

1. **⚠️ 不要硬编码选项** - 必须使用系统字典
2. **⚠️ 导入路径** - `dict` 从 `@fast-crud/fast-crud`, `dictionary` 从 `/@/utils/dictionary`
3. **字典编码** - 必须与前端使用的 key 一致

---

## 通用组件开发规范

### 1. 什么是通用组件?

**定义：** 可以在多个页面或模块中复用的 UI 组件，通常封装特定的业务逻辑和展示方式。

**示例：**
- 工单详情弹窗 - 任何页面都可以打开查看工单详情
- 用户选择器 - 选择用户的通用组件
- 文件上传组件 - 统一的文件上传UI

---

### 2. ⚠️ 关键规范（基于实战经验）

#### 2.1 组件引入方式

**❌ 错误方式：动态挂载**
```typescript
// ❌ 不要这样做
const { openDetail } = useComplaintDetail()  // Composable 动态挂载组件

// ❌ useComplaintDetail.ts
function mountComponent() {
  const container = document.createElement('div')
  document.body.appendChild(container)
  const app = createApp(ComplaintDetailComponent)
  app.mount(container)  // 动态挂载不符合项目规范
}
```

**✅ 正确方式：defineAsyncComponent + ref**
```vue
<!-- ✅ 在父组件模板中引入 -->
<template>
  <fs-page>
    <fs-crud ref="crudRef" v-bind="crudBinding" />
    <!-- 引入通用组件 -->
    <ComplaintDetail ref="complaintDetailRef" />
  </fs-page>
</template>

<script setup>
import { defineAsyncComponent } from 'vue'

// ✅ 使用 defineAsyncComponent 动态导入
const ComplaintDetail = defineAsyncComponent(() =>
  import('/@/components/ComplaintDetail/index.vue')
)
const complaintDetailRef = ref()

// ✅ 通过 ref 传递给 CRUD 配置
const { crudOptions } = createCrudOptions({
  crudExpose,
  context: {},  // 必须传入 context（即使为空）
  complaintDetailRef  // 传递组件 ref
})
</script>
```

**原因：**
1. **符合项目规范** - 参考 `avatarSelector` 等现有组件
2. **生命周期管理** - Vue 自动管理组件生命周期
3. **类型安全** - TypeScript 类型推导正确
4. **性能更好** - 无需手动创建 DOM 节点

---

#### 2.2 在 CRUD 配置中使用组件

**✅ 标准方式：接收 ref 参数**
```typescript
// crud.tsx
import type { Ref } from 'vue'

// ✅ 扩展参数类型
export const createCrudOptions = function (
  props: CreateCrudOptionsProps & { complaintDetailRef?: Ref<any> }
): CreateCrudOptionsRet {
  const { crudExpose, complaintDetailRef } = props

  return {
    crudOptions: {
      rowHandle: {
        buttons: {
          view: {
            click: ({ row }: any) => {
              // ✅ 通过 ref 调用组件方法
              if (complaintDetailRef && complaintDetailRef.value) {
                complaintDetailRef.value.open(row.complaint_id)
              }
            }
          }
        }
      }
    }
  }
}
```

**关键要点：**
1. **参数类型扩展** - `CreateCrudOptionsProps & { complaintDetailRef?: Ref<any> }`
2. **解构参数** - `const { crudExpose, complaintDetailRef } = props`
3. **安全调用** - 先检查 `complaintDetailRef.value` 是否存在

---

#### 2.3 API 响应数据处理

**⚠️ 项目标准响应格式：**
```json
{
  "code": 2000,
  "msg": "获取成功",
  "data": { /* 实际数据 */ }
}
```

**后端必须使用 DetailResponse 包装**
```python
# ❌ 错误 - 直接返回数据
from rest_framework.response import Response

return Response(serializer.data)  # 非标准格式

# ✅ 正确 - 使用 DetailResponse
from dvadmin.utils.json_response import DetailResponse

return DetailResponse(data=serializer.data, msg="获取成功")
```

**前端必须提取 data 字段**
```typescript
// ❌ 错误 - 直接使用响应
const data = await GetFullDetail(id)
detail.value = data  // 错误：data 包含 code/msg/data

// ✅ 正确 - 提取 data 字段
const response = await GetFullDetail(id)
detail.value = response.data || response  // 正确
```

**API 类型定义：**
```typescript
// ✅ 明确定义响应格式
interface ApiResponse<T> {
  code: number
  msg: string
  data: T
}

export function GetFullDetail(id: string): Promise<ApiResponse<ComplaintFullDetail>> {
  return request({
    url: `${apiPrefix}/${id}/full-detail/`,
    method: 'get'
  })
}
```

---

#### 2.4 Element Plus 组件自动导入

**⚠️ 无需显式导入 Element Plus 组件**

项目已配置自动导入，以下组件可以直接使用：

```vue
<template>
  <!-- ✅ 直接使用，无需导入 -->
  <el-dialog v-model="visible">
    <el-skeleton :loading="loading">
      <el-descriptions :column="2" border>
        <el-descriptions-item label="标题">
          {{ detail.title }}
        </el-descriptions-item>
      </el-descriptions>
      <el-tag type="danger">风险</el-tag>
    </el-skeleton>
  </el-dialog>
</template>

<script setup>
// ❌ 不需要这样导入
// import { ElDialog, ElSkeleton, ElTag } from 'element-plus'

// ✅ 只需导入业务逻辑相关的模块
import { ref } from 'vue'
import { GetFullDetail } from './api'
</script>
```

**自动导入的组件包括：**
- `el-dialog`, `el-drawer` - 弹窗和抽屉
- `el-form`, `el-input`, `el-select` - 表单组件
- `el-table`, `el-pagination` - 表格组件
- `el-button`, `el-tag`, `el-badge` - 基础组件
- `el-skeleton`, `el-descriptions` - 展示组件
- 更多...

---

### 3. 通用组件开发原则

#### 3.1 组件目录结构

**✅ 标准结构：**
```
web/src/components/ComplaintDetail/
├── index.vue                  # 主组件（弹窗/抽屉）
├── api.ts                     # API 接口定义
└── types.ts                   # TypeScript 类型定义
```

**⚠️ 注意：**
- ❌ 不再创建 `useComplaintDetail.ts` - 不符合项目规范
- ✅ 使用 `defineAsyncComponent` + `ref` 方式调用

#### 3.2 关键文件说明

**index.vue - 主组件**
- 使用 `<script setup>` 语法
- 通过 `defineExpose` 暴露公共方法
- 自包含，不依赖外部状态
- 支持弹窗（Dialog）或抽屉（Drawer）形式

**api.ts - API 接口**
- 独立函数导出
- TypeScript 类型定义（包含 ApiResponse 包装）
- 错误处理

**types.ts - 类型定义**
- 导出所有接口和类型
- 清晰的字段注释

---

### 4. 完整示例：工单详情组件（修订版）

#### 4.1 创建类型定义

```typescript
// web/src/components/ComplaintDetail/types.ts

/**
 * 风险信息
 */
export interface RiskInfo {
  id: number
  is_risk: boolean
  risk_category: string | null
  risk_reason: string | null
  created_at: string | null
}

/**
 * 工单完整详情
 */
export interface ComplaintFullDetail {
  // 主键
  id: string
  // 基础信息
  complainant_name: string | null
  complainant_phone: string | null
  // 诉求内容
  title: string | null
  content: string | null
  // ... 更多字段
  // 关联信息
  risk_info: RiskInfo | null
}
```

#### 4.2 创建 API 接口

```typescript
// web/src/components/ComplaintDetail/api.ts

import { request } from '/@/utils/service'
import type { ComplaintFullDetail } from './types'

export const apiPrefix = '/api/complaint/complaints'

/**
 * 标准响应格式
 */
interface ApiResponse<T> {
  code: number
  msg: string
  data: T
}

/**
 * 获取工单完整详情
 */
export function GetFullDetail(id: string): Promise<ApiResponse<ComplaintFullDetail>> {
  return request({
    url: `${apiPrefix}/${id}/full-detail/`,
    method: 'get'
  })
}
```

#### 4.3 创建主组件

```vue
<!-- web/src/components/ComplaintDetail/index.vue -->
<template>
  <el-dialog
    v-model="visible"
    title="工单详情"
    width="1200px"
    top="5vh"
    :close-on-click-modal="false"
    destroy-on-close
  >
    <el-skeleton :loading="loading" animated>
      <template #default>
        <div v-if="detail" class="complaint-detail">
          <!-- 基础信息 -->
          <el-divider content-position="left">基础信息</el-divider>
          <el-descriptions :column="2" border>
            <el-descriptions-item label="工单编号">
              {{ detail.id }}
            </el-descriptions-item>
            <el-descriptions-item label="诉求标题">
              {{ detail.title || '-' }}
            </el-descriptions-item>
          </el-descriptions>

          <!-- 风险信息 -->
          <template v-if="detail.risk_info">
            <el-divider content-position="left">风险信息</el-divider>
            <el-descriptions :column="2" border>
              <el-descriptions-item label="是否风险">
                <el-tag :type="detail.risk_info.is_risk ? 'danger' : 'success'">
                  {{ detail.risk_info.is_risk ? '是' : '否' }}
                </el-tag>
              </el-descriptions-item>
              <el-descriptions-item label="风险类别">
                {{ detail.risk_info.risk_category || '-' }}
              </el-descriptions-item>
            </el-descriptions>
          </template>
        </div>
      </template>
    </el-skeleton>

    <template #footer>
      <el-button @click="visible = false">关闭</el-button>
    </template>
  </el-dialog>
</template>

<script lang="ts" setup name="ComplaintDetail">
import { ref } from 'vue'
import { GetFullDetail } from './api'
import type { ComplaintFullDetail } from './types'

const visible = ref(false)
const loading = ref(false)
const detail = ref<ComplaintFullDetail | null>(null)

/**
 * 打开详情弹窗
 */
const open = async (complaintId: string) => {
  visible.value = true
  loading.value = true
  detail.value = null

  try {
    const response = await GetFullDetail(complaintId)
    // ✅ 从响应中提取 data 字段
    detail.value = response.data || response
  } catch (error) {
    console.error('获取工单详情失败:', error)
  } finally {
    loading.value = false
  }
}

/**
 * 关闭详情弹窗
 */
const close = () => {
  visible.value = false
}

// 暴露方法给父组件
defineExpose({
  open,
  close
})
</script>

<style lang="scss" scoped>
.complaint-detail {
  .el-divider {
    margin-top: 24px;
    margin-bottom: 16px;

    &:first-child {
      margin-top: 0;
    }
  }
}
</style>
```

---

### 5. 在业务页面中使用

#### 5.1 在主页面中引入组件

```vue
<!-- web/src/views/risk/warning/index.vue -->
<template>
  <fs-page class="risk-warning-container">
    <fs-crud ref="crudRef" v-bind="crudBinding" />
    <!-- ✅ 引入工单详情组件 -->
    <ComplaintDetail ref="complaintDetailRef" />
  </fs-page>
</template>

<script lang="ts" setup name="riskWarning">
import { onMounted, ref, defineAsyncComponent } from 'vue'
import { useExpose, useCrud } from '@fast-crud/fast-crud'
import { createCrudOptions } from './crud'

// ✅ 使用 defineAsyncComponent 动态导入
const ComplaintDetail = defineAsyncComponent(() =>
  import('/@/components/ComplaintDetail/index.vue')
)
const complaintDetailRef = ref()

// crud组件的ref
const crudRef = ref()
const crudBinding = ref()
// 暴露的方法
const { crudExpose } = useExpose({ crudRef, crudBinding })

// ✅ 传递 complaintDetailRef 到 crud 配置
const { crudOptions } = createCrudOptions({
  crudExpose,
  context: {},  // 必须传入（即使为空）
  complaintDetailRef
})

// 初始化crud配置
useCrud({ crudExpose, crudOptions })

// 页面打开后获取列表数据
onMounted(() => {
  crudExpose.doRefresh()
})
</script>
```

#### 5.2 在 CRUD 配置中集成

```typescript
// web/src/views/risk/warning/crud.tsx
import type { Ref } from 'vue'

export const createCrudOptions = function (
  props: CreateCrudOptionsProps & { complaintDetailRef?: Ref<any> }
): CreateCrudOptionsRet {
  const { crudExpose, complaintDetailRef } = props

  return {
    crudOptions: {
      // 行操作配置
      rowHandle: {
        buttons: {
          view: {
            show: true,
            text: '查看',
            type: 'text',
            click: ({ row }: any) => {
              // ✅ 通过 ref 调用组件方法
              if (complaintDetailRef && complaintDetailRef.value) {
                complaintDetailRef.value.open(row.complaint_id)
              }
            }
          }
        }
      }
    }
  }
}
```

---

### 6. 通用组件开发检查清单

- [ ] **独立性** - 组件不依赖外部状态或上下文
- [ ] **类型安全** - 完整的 TypeScript 类型定义
- [ ] **API 独立** - API 接口独立文件，函数导出
- [ ] **响应处理** - 正确提取 `response.data` 字段
- [ ] **后端规范** - 使用 `DetailResponse` 包装返回数据
- [ ] **组件引入** - 使用 `defineAsyncComponent` + ref 方式
- [ ] **文档注释** - 清晰的函数和参数注释
- [ ] **错误处理** - 完善的错误处理和提示
- [ ] **加载状态** - 骨架屏或 loading 提示
- [ ] **响应式** - 自适应不同屏幕尺寸
- [ ] **Element Plus** - 无需显式导入，直接使用

---

### 7. 后端通用接口开发

#### 6.1 创建独立插件

```python
# backend/plugins/complaint/models.py
class Complaint(models.Model):
    """工单模型"""
    id = models.CharField(max_length=50, primary_key=True)
    title = models.CharField(max_length=500, null=True, blank=True)
    # ... 更多字段

    class Meta:
        db_table = 'complaints'
        managed = False
```

#### 6.2 创建详情序列化器

```python
# backend/plugins/complaint/serializers.py
from dvadmin.utils.serializers import CustomModelSerializer

class ComplaintFullDetailSerializer(CustomModelSerializer):
    """工单完整详情序列化器"""
    risk_info = serializers.SerializerMethodField()

    class Meta:
        model = Complaint
        fields = '__all__'
        read_only_fields = fields

    def get_risk_info(self, obj):
        """获取关联的风险信息"""
        risk_tag = obj.risk_tags.first()
        if risk_tag:
            return {
                'id': risk_tag.id,
                'is_risk': risk_tag.is_risk,
                'risk_category': risk_tag.risk_category,
                'risk_reason': risk_tag.risk_reason,
                'created_at': risk_tag.created_at
            }
        return None
```

#### 6.3 创建详情视图

```python
# backend/plugins/complaint/views.py
from rest_framework.decorators import action
from rest_framework.response import Response
from dvadmin.utils.viewset import CustomModelViewSet

class ComplaintViewSet(CustomModelViewSet):
    """工单视图集"""
    queryset = Complaint.objects.all()
    serializer_class = ComplaintFullDetailSerializer

    @action(methods=['get'], detail=True, url_path='full-detail')
    def full_detail(self, request, pk=None):
        """获取完整的工单详情"""
        complaint = self.get_queryset().prefetch_related('risk_tags').get(pk=pk)
        serializer = self.get_serializer(complaint)
        return Response(serializer.data)
```

#### 6.4 注册 URL 和应用

```python
# backend/plugins/complaint/urls.py
router = DefaultRouter()
router.register(r'complaints', ComplaintViewSet, basename='complaint')

# application/settings.py
INSTALLED_APPS = [
    # ...
    "plugins.complaint",  # 添加工单插件
]

PLUGINS_URL_PATTERNS = [
    {
        're_path': r'^api/complaint/',
        'include': 'plugins.complaint.urls'
    }
]
```

---

## 数据库规范

### 1. 表设计规范

#### 1.1 基本要求

- **主键命名**: 使用 `id`，自增整数或字符串
- **时间字段**: `created_at`, `updated_at` (DATETIME)
- **外键字段**: 使用 `{关联表名}_id` 命名
- **布尔字段**: 使用 `is_xxx` 命名（如 `is_risk`）
- **索引**: 为外键和常用查询字段添加索引

#### 1.2 字段类型选择

| 用途 | 推荐类型 | 示例 |
|------|---------|------|
| 主键 | INT AUTO_INCREMENT 或 VARCHAR(50) | id |
| 布尔值 | TINYINT(1) | is_risk |
| 短文本 | VARCHAR(255) | title, category |
| 长文本 | TEXT | content, reason |
| 日期时间 | DATETIME | created_at, accept_time |
| 枚举 | VARCHAR(50) + 字典表 | status, type |

---

## 命名规范

### 1. 后端命名

#### Python 代码
- **类名**: PascalCase（大驼峰）- `RiskTag`, `RiskTagViewSet`
- **函数/方法**: snake_case（蛇形）- `get_queryset`, `export_data`
- **变量**: snake_case - `risk_category`, `accept_time`
- **常量**: UPPER_SNAKE_CASE - `API_VERSION`, `MAX_LENGTH`

#### 数据库
- **表名**: snake_case + 复数 - `risk_tags`, `complaints`
- **字段名**: snake_case - `complaint_id`, `is_risk`, `created_at`

### 2. 前端命名

#### TypeScript 代码
- **接口/类型**: PascalCase - `RiskTag`, `RiskTagQuery`
- **函数**: PascalCase（API函数）- `GetList`, `GetObj`, `Export`
- **变量**: camelCase（小驼峰）- `crudRef`, `riskCategory`
- **常量**: UPPER_SNAKE_CASE - `API_PREFIX`, `PAGE_SIZE`

#### 文件和路径
- **文件名**: camelCase - `index.vue`, `crud.tsx`, `api.ts`
- **URL路径**: kebab-case - `/api/risk-warning/risk-tags/`
- **组件名**: PascalCase - `RiskWarning`, `UserManagement`

---

## 文件组织规范

### 1. 后端文件结构

```
backend/
├── plugins/
│   └── risk_warning/              # 插件目录
│       ├── __init__.py
│       ├── apps.py                # 应用配置
│       ├── models.py              # 数据模型
│       ├── serializers.py         # 序列化器
│       ├── views.py               # 视图集
│       └── urls.py                # 路由配置
├── application/
│   ├── settings.py                # 全局配置
│   └── db_router.py               # 数据库路由器
└── dvadmin/
    └── utils/                     # 工具类
        ├── models.py              # CoreModel
        ├── serializers.py         # CustomModelSerializer
        └── viewset.py             # CustomModelViewSet
```

### 2. 前端文件结构

```
web/
├── src/
│   ├── views/
│   │   └── risk/
│   │       └── warning/           # 功能模块目录
│   │           ├── index.vue      # 主页面组件
│   │           ├── crud.tsx       # CRUD 配置
│   │           └── api.ts         # API 接口
│   ├── utils/
│   │   ├── dictionary.ts          # 字典工具
│   │   └── service.ts             # 请求封装
│   └── stores/
│       └── dictionary.ts          # 字典状态管理
```

---

## 完整开发流程示例

### 步骤1：创建后端模型

```python
# plugins/risk_warning/models.py
from django.db import models

class RiskTag(models.Model):
    complaint = models.ForeignKey('Complaint', on_delete=models.DO_NOTHING)
    is_risk = models.BooleanField(default=False)
    risk_category = models.CharField(max_length=50, null=True)

    class Meta:
        db_table = 'risk_tags'
        managed = False
        verbose_name = '风险标注'
```

### 步骤2：创建序列化器

```python
# plugins/risk_warning/serializers.py
from dvadmin.utils.serializers import CustomModelSerializer
from .models import RiskTag

class RiskTagSerializer(CustomModelSerializer):
    complaint_id = serializers.CharField(source='complaint.id', read_only=True)

    class Meta:
        model = RiskTag
        fields = '__all__'
        read_only_fields = fields
```

### 步骤3：创建视图集

```python
# plugins/risk_warning/views.py
from dvadmin.utils.viewset import CustomModelViewSet
from .models import RiskTag
from .serializers import RiskTagSerializer

class RiskTagViewSet(CustomModelViewSet):
    queryset = RiskTag.objects.select_related('complaint').all()
    serializer_class = RiskTagSerializer
    http_method_names = ['get', 'head', 'options']
```

### 步骤4：配置路由

```python
# plugins/risk_warning/urls.py
from rest_framework.routers import DefaultRouter
from .views import RiskTagViewSet

router = DefaultRouter()
router.register(r'risk-tags', RiskTagViewSet, basename='risk-tags')
urlpatterns = router.urls
```

### 步骤5：注册应用

```python
# application/settings.py
INSTALLED_APPS = [
    # ...
    "plugins.risk_warning",
]

PLUGINS_URL_PATTERNS = [
    {
        're_path': r'^api/risk_warning/',
        'include': 'plugins.risk_warning.urls'
    }
]
```

### 步骤6：创建系统字典

1. 登录后台管理
2. 系统管理 > 字典管理
3. 创建父字典：`risk_category`
4. 添加子字典项

### 步骤7：创建前端 API

```typescript
// web/src/views/risk/warning/api.ts
export function GetList(query: RiskTagQuery) {
  return request({
    url: '/api/risk_warning/risk-tags/',
    method: 'get',
    params: query
  })
}
```

### 步骤8：创建 CRUD 配置

```typescript
// web/src/views/risk/warning/crud.tsx
import { dict } from '@fast-crud/fast-crud'
import { dictionary } from '/@/utils/dictionary'
import * as api from './api'

export const createCrudOptions = function ({ crudExpose }) {
  const pageRequest = async (query) => {
    return await api.GetList(query)
  }

  return {
    crudOptions: {
      request: { pageRequest },
      columns: {
        risk_category: {
          type: 'dict-select',
          dict: dict({ data: dictionary('risk_category') })
        }
      }
    }
  }
}
```

### 步骤9：创建主页面

```vue
<!-- web/src/views/risk/warning/index.vue -->
<template>
  <fs-page>
    <fs-crud ref="crudRef" v-bind="crudBinding" />
  </fs-page>
</template>

<script lang="ts" setup name="riskWarning">
import { useExpose, useCrud } from '@fast-crud/fast-crud'
import { createCrudOptions } from './crud'

const crudRef = ref()
const crudBinding = ref()
const { crudExpose } = useExpose({ crudRef, crudBinding })
const { crudOptions } = createCrudOptions({ crudExpose })
useCrud({ crudExpose, crudOptions })
</script>
```

### 步骤10：配置路由

```typescript
// web/src/router/modules/risk.ts
{
  path: '/risk/warning',
  name: 'RiskWarning',
  component: () => import('/@/views/risk/warning/index.vue'),
  meta: {
    title: '风险预警',
    icon: 'Warning'
  }
}
```

---

## 常见错误和解决方案

### 错误1：crudExpose不能为空

**错误信息：**
```
Uncaught Error: crudExpose不能为空，请给useCrud传入{crudExpose}参数
```

**原因：**
没有正确使用 `useExpose` 创建 `crudExpose`

**解决方案：**
```typescript
// ❌ 错误写法
const { crudBinding, crudRef, crudExpose } = useCrud({ createCrudOptions })

// ✅ 正确写法
const crudRef = ref()
const crudBinding = ref()
const { crudExpose } = useExpose({ crudRef, crudBinding })
const { crudOptions } = createCrudOptions({ crudExpose })
useCrud({ crudExpose, crudOptions })
```

---

### 错误2：r.loadDict is not a function

**错误信息：**
```
TypeError: r.loadDict is not a function
```

**原因：**
使用了 `dict-select` 但没有正确配置字典

**解决方案：**
```typescript
// ❌ 错误写法
type: 'dict-select',
dict: { data: [...] }

// ✅ 正确写法
import { dict } from '@fast-crud/fast-crud'
import { dictionary } from '/@/utils/dictionary'

type: 'dict-select',
dict: dict({
  data: dictionary('risk_category')
})
```

---

### 错误3：Cannot read properties of undefined (reading 'sentence')

**错误信息：**
```
contentScript.js:139 Uncaught (in promise) TypeError
```

**原因：**
这是浏览器扩展的错误，与代码无关

**解决方案：**
忽略此错误，或禁用相关浏览器扩展

---

### 错误4：分页数据不显示

**错误信息：**
API 成功返回数据，但列表不显示

**原因：**
响应格式映射不正确

**解决方案：**
```typescript
// ✅ 添加 responseMap
request: {
  pageRequest,
  responseMap: {
    data: 'data',
    currentPage: 'page',
    pageSize: 'limit',
    total: 'total'
  }
}
```

---

### 错误5：Field.__init__() got unexpected keyword argument 'request'

**错误信息：**
```
TypeError: Field.__init__() got unexpected keyword argument 'request'
```

**原因：**
使用了 `ModelSerializer` 而非 `CustomModelSerializer`

**解决方案：**
```python
# ❌ 错误写法
from rest_framework import serializers
class RiskTagSerializer(serializers.ModelSerializer):
    pass

# ✅ 正确写法
from dvadmin.utils.serializers import CustomModelSerializer
class RiskTagSerializer(CustomModelSerializer):
    pass
```

---

### 错误6：NULL 值序列化失败

**错误信息：**
```
This field may not be null.
```

**原因：**
字段没有正确处理 NULL 值

**解决方案：**
```python
# ✅ 添加 NULL 值处理
complaint_title = serializers.CharField(
    source='complaint.title',
    read_only=True,
    required=False,      # 允许缺失
    allow_null=True,     # 允许 NULL
    allow_blank=True     # 允许空字符串
)
```

---

### 错误7：Blob 文件下载失败

**错误信息：**
```
TypeError: Failed to execute 'createObjectURL' on 'URL'
```

**原因：**
响应拦截器返回完整 response 对象，需要提取 Blob

**解决方案：**
```typescript
// ✅ 从 response.data 提取 Blob
export async function Export(query: RiskTagQuery): Promise<Blob> {
  const response: any = await request({
    url: apiPrefix + '/export/',
    method: 'get',
    params: query,
    responseType: 'blob'
  })
  return response.data || response  // ← 关键
}
```

---

## 检查清单

### 后端开发检查清单

- [ ] 模型继承正确（CoreModel 或 models.Model）
- [ ] 序列化器继承 CustomModelSerializer
- [ ] 视图集继承 CustomModelViewSet
- [ ] 使用 select_related 优化查询
- [ ] FilterSet 配置完整
- [ ] URL 注册正确
- [ ] 多数据库路由配置（如需要）
- [ ] 所有字段有 verbose_name
- [ ] Meta 类配置完整
- [ ] 添加了必要的注释

### 前端开发检查清单

- [ ] 使用三文件结构（index.vue, crud.tsx, api.ts）
- [ ] API 函数独立导出（不用对象）
- [ ] CRUD 配置命名导出 createCrudOptions
- [ ] 使用 CreateCrudOptionsProps 类型
- [ ] 不使用 transformQuery/transformRes
- [ ] 使用 responseMap 配置
- [ ] 字典使用 dict(dictionary('key'))
- [ ] 使用 useExpose + useCrud 初始化
- [ ] 使用 `<script setup>` 语法
- [ ] 导入方式正确（import * as api）

---

## 总结

### 核心原则

1. **遵循项目标准** - 使用项目的基类和工具
2. **不要重复造轮** - 使用已有的功能和配置
3. **保持一致性** - 与其他模块保持相同的代码风格
4. **注重可维护性** - 代码清晰、注释完整
5. **类型安全** - 充分利用 TypeScript 的类型系统

### 最佳实践

1. **后端**：
   - 继承 CustomModelSerializer 和 CustomModelViewSet
   - 使用 select_related 优化查询
   - 使用 FilterSet 实现复杂筛选
   - 添加详细的文档注释

2. **前端**：
   - 使用系统字典管理选项数据
   - 使用 responseMap 而非 transform
   - 遵循三步初始化流程
   - API 函数独立导出
   - 通用组件独立开发，使用 Composable

3. **架构**：
   - 遵循插件独立性原则
   - 通用功能独立成插件
   - 统一使用默认数据库
   - 参考标准模块（system/user、system/role）

4. **通用**：
   - 遇到问题先搜索现有代码
   - 保持命名规范一致
   - 代码审查确保质量

---

**文档版本**: v2.0
**初始版本**: v1.0 (2025-11-27)
**最后更新**: v2.0 (2025-11-27)
**更新内容**:
- 新增"架构设计原则"章节
- 新增"通用组件开发规范"章节
- 新增工单详情组件完整开发案例
- 移除多数据库配置（统一使用默认数据库）
- 新增插件独立性原则说明

**维护者**: Claude (Opus 4.5)
**参考项目**: 长春市12345情感分析平台后台管理系统
